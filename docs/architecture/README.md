# Architecture records

This directory contains accepted architecture descriptions for the repository.
Material design decisions belong in `docs/decisions/`; work that spans sessions
or needs recovery belongs in one file under `docs/plans/active/`.

Implementation agents must follow accepted records and must not infer new policy
from code conventions. Drafts produced by `wf-discover` remain proposals until
H1 approval.
